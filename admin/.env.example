# 后台管理前端：商城 API 根路径（须带 /api 前缀，不要末尾斜杠）
# 生产构建：复制本文件为 .env.production 并填写，或构建时用环境变量覆盖
# 例（与 Nginx 反代一致）：VITE_MALL_API_BASE=http://你的公网IP或域名/api
VITE_MALL_API_BASE=http://47.96.89.158/api
#
# 子系统标识（可选）：留空时前端自动从子域名首段提取（如 a.example.com → a），
# 本地/IP 默认回退为 default。需要固定某子系统时可显式填写。
# VITE_TENANT_ID=default

# 流量管理「推广链接」指向商城 H5（shop）。未设置时的回退：
# - 本地：admin 默认 5174 时会指向同主机 5173（shop vite 默认端口）
# - 生产：若当前后台为 http://主机:8080，会指向 http://主机（默认 80，无 :8080）
# 若商城与后台不同域名或端口规律不同，请显式配置（不要末尾斜杠），例如：
# VITE_MALL_H5_ORIGIN=http://116.62.64.224
# VITE_MALL_H5_ORIGIN=https://mall.example.com

# 默认授信额度（元）→ 见 api/.env.development / api/.env.production，勿在 admin 重复配置

# 本地开发：若不设置，代码会退回 http://localhost:3110/api
# 若本机 api 在 3110，可写：
# VITE_MALL_API_BASE=http://127.0.0.1:3110/api

# 平台账号用户名白名单（逗号分隔）：当后端旧版本未返回 scopeType 时，
# 前端将 super_admin 且用户名命中该白名单的会话兜底识别为平台账号，避免 /platform 被重定向。
# VITE_PLATFORM_USERNAMESVITE_PLATFORM_USERNAMES=xuyang
