# 后台管理前端：商城 API 根路径（须带 /api 前缀，不要末尾斜杠）
# 生产构建：复制本文件为 .env.production 并填写，或构建时用环境变量覆盖
# 例（与 Nginx 反代一致）：VITE_MALL_API_BASE=http://你的公网IP或域名/api
VITE_MALL_API_BASE=http://47.96.89.158/api
#
# 租户标识（可选）：留空时前端自动从子域名首段提取（如 a.example.com → a），
# 本地/IP 默认回退为 default。需要固定某租户时可显式填写。
# VITE_TENANT_ID=default

# 流量管理页「复制推广链接」用的 H5（商城）站点根地址（不要末尾斜杠），须与实际打开的商城入口一致。
# 若留空，将误用「当前浏览器标签页的 origin」——本地开发时常见后台在 5173、商城在 5174，会变成错误的端口。
# 复制本文件为 .env.development 并取消下一行注释（端口按你本机商城 Vite 为准，常见 5173；本项目示例为 5174）：
# VITE_MALL_H5_ORIGIN=http://localhost:5174
# 生产示例：
# VITE_MALL_H5_ORIGIN=http://47.96.89.158

# 本地开发：若不设置，代码会退回 http://localhost:3110/api
# 若本机 api 在 3110，可写：
# VITE_MALL_API_BASE=http://127.0.0.1:3110/api

# 平台账号用户名白名单（逗号分隔）：当后端旧版本未返回 scopeType 时，
# 前端将 super_admin 且用户名命中该白名单的会话兜底识别为平台账号，避免 /platform 被重定向。
# VITE_PLATFORM_USERNAMES=xuyang
