# 项目协作要求

## Superpowers 使用要求

- 只要任务涉及修改任何文件，尤其是接口逻辑、后端路由、数据写入、环境变量、前端表单提交流程，必须先使用用户的 `superpowers` skill 工作流。
- 涉及接口逻辑或业务数据变更时，必须优先进行计划、验证和风险检查，不能直接改动上线业务。
- 修改完成后必须说明：
  - 修改了哪些文件；
  - 是否影响已有业务和数据；
  - 使用了哪些验证命令；
  - 验证是否通过。
- 如果当前 Codex 会话没有自动列出 `superpowers` skill，需要先检查本机 `C:\Users\xu\.codex\superpowers\skills` 下的相关 skill 说明，并按其工作流执行。

## 上线项目保护原则

- 不得重置、删除、迁移或覆盖线上业务数据，除非用户明确要求。
- 新增能力应尽量采用独立文件、独立路由或独立配置，避免混入已有接口逻辑。
- 可变参数必须优先放入环境变量或配置文件管理，不要写死在代码里。
- 所有中文注释、提示和环境变量备注必须保持 UTF-8，不得出现乱码。
