# 生产 / 直连后端时填写完整 mall API 前缀（不要尾斜杠外的多余路径）
# 例：VITE_MALL_API_BASE=http://localhost:3110/api
VITE_MALL_API_BASE=

# 本地 `npm run dev` 时若不设置 VITE_MALL_API_BASE：
#   - 前端请求会发到同源 `/api/*`
#   - 由 Vite 代理到后端（默认 http://127.0.0.1:3110）
# 若你的 API 端口不是 3110，可改启动命令前的环境变量：
#   SHOP_API_PROXY_TARGET=http://127.0.0.1:YOUR_PORT npm run dev
