# =============================================================================
# 独立部署（新服务器）：流量商只看本机域名/IP，数据与主系统共用 MongoDB
# =============================================================================
#
# 1) 新服务器跑 api（与主服务器 api 是两套进程，MONGODB_URI 填同一个云库）
# 2) 本目录 build 后 Nginx 托管 dist，VITE_MALL_API_BASE 指向新服务器自己的 /api
# 3) 主服务器继续跑 api + admin + shop，勿停；两边 API 同时写同一库即可数据打通
#
# 构建前复制为 .env.production：
# VITE_MALL_API_BASE=http://你的新服务器公网IP/api
# 或 https://liuliang.你的独立域名/api
#
# VITE_MALL_H5_ORIGIN=https://你的商城H5域名
# （推广链接指向商城，可与 admin 后台域名完全不同；勿填主后台域名）
#
# VITE_TENANT_ID=default
# （须与主系统 shop/.env.production 里 VITE_TENANT_ID 一致）
#
# 本地开发：
VITE_MALL_API_BASE=http://127.0.0.1:3110/api
