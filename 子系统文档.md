# LOAN · 多租户商城系统架构（精简）

## 置顶：不同租户需要关注的配置差异

下表约定：**租户标识**为小写字母、数字、下划线、`_`、`-`，最长 64 字符；默认租户 id 为 `default`（对应 Mongo「根库」，库名通常为 `MONGODB_DB_NAME`，如 `mall`）。

### 商城 `shop/`（用户 H5 / 前台）

| 关注点 | 说明 |
| --- | --- |
| **`VITE_TENANT_ID`** | **按租户最关键**：指明当前构建/运行访问的是哪个子系统。留空时在**非 localhost、非裸 IP** 下会尝试从**主机名首段子域名**解析（如 `a.example.com` → `a`）；本地/IP 默认回退 `default`。 |
| **`VITE_MALL_API_BASE`** | 浏览器访问的 API 根路径（须带 `/api` 后缀形式与项目约定一致）。生产与各租户入口域名、`/api` 反代保持一致。 |
| **开发代理** | `SHOP_API_PROXY_TARGET`（见 `shop` 环境文件）指向本机后端端口，便于 `Vite` 将 `/api` 转到正确 API。 |

前端会为 `fetch` 自动补上请求头 **`x-tenant-id`**（与上方解析出的租户一致），后端优先认该头。

### 后台 `admin/`（运营管理）

| 关注点 | 说明 |
| --- | --- |
| **`VITE_TENANT_ID`** | 与子系统对齐；与子域名推断规则与 `shop` 相同。**租户级管理员账号**会话下，服务端还会以账号所属租户为准，与请求租户不一致时会纠偏。 |
| **`VITE_MALL_API_BASE`** | 管理端调用的商城 API 基址（带 `/api`）。 |
| **`VITE_MALL_H5_ORIGIN`** | 「复制推广链接」等能力依赖的 **H5 根地址**（无末尾 `/`）。**勿依赖当前浏览器标签 origin**——本地常见后台与商城端口不同，必须显式配置，否则会生成错误链接。 |
| **`VITE_PLATFORM_USERNAMES`** | 可选；旧后端未返回 `scopeType` 时，配合超管用户名白名单兜底识别**平台账号**，避免路由误判。 |

平台账号可调 **`x-workspace-type`**：`core`（总部元数据，如租户清单）、`self`、`tenant`，对应后端不同 Mongo 库作用域（见下文）。普通租户管理员稳定在 `tenant` 工作区。

### 服务端 `api/`（单机多租户）

| 关注点 | 说明 |
| --- | --- |
| **租户解析顺序** | 请求头 `x-tenant-id`（及兼容头）→ URL 查询参数 `tenantId` / `tenant` 等 → **Host 子域名首段** → 默认 `default`。 |
| **数据隔离（Mongo）** | `tenant` 工作区：`default` 使用根库；其它租户使用 **`{MONGODB_DB_NAME}__tenant_{tenantId}`**。另有 `mall__core`、`mall__self` 等平台/自建工作库（与工作区类型相关）。 |
| **进程级环境变量** | `MONGODB_URI`、`风控/华东云`、`OSS`、默认短信模板签名等一般由 **同一 API 进程共享**。若两个租户要强隔离的第三方密钥、短信签名栈、OSS 桶等，通常需要 **多套部署或多套环境**，而不是单靠改租户 id。 |

详细键名见 `api/.env.example`。

---

## 仓库与职责

| 目录 | 角色 |
| --- | --- |
| **`shop/`** | 商城 C 端（Vue + Vite），订单、注册、下单等面向用户。 |
| **`admin/`** | 后台管理（Vue + Vite），订单审核、商品、流量、客服、租户/账号（平台能力）等。 |
| **`api/`** | 单体式 Node HTTP 服务，统一 `/api` 业务与持久化（Mongo + 异步上下文中的租户/workspace）。 |

三者通过 **同源或显式配置的 API Base** + **`x-tenant-id`（及后台的 `x-workspace-type`）** 串通请求作用域。

## 请求链路（概念）

```
浏览器 (shop/admin)
  → 注入 x-tenant-id（及管理员相关的 x-workspace-type）
    → api 解析租户 / 工作区
      → mongo.js 选择 db：根库 | __tenant_* | __core | __self
        → 业务读写与返回
```

## 延伸阅读

- API 与环境变量全貌：`api/.env.example`、`api/API.md`
- 管理端环境模板：`admin/.env.example`
