---
description: 后端/API/数据层：查询与接口设计（通用）
globs: "**/*.{js,ts,mjs,cjs,py,go,java,kt,cs,rb,php}"
alwaysApply: false
---

# API 与数据层规范（通用）

## 接口设计

- **一个业务场景一个接口**：列表用 `page/pageSize/filters`，详情用 `id`；禁止为省事复用「返回整库」的巨型接口。
- **字段按需**：列表 DTO 不含大 JSON blob、历史消息全文、base64。
- **写操作**：明确幂等键或状态机；禁止「先查全表再改一条」。

## 数据访问反模式（禁止除非文档化例外）

```text
❌ SELECT * / find({}) 后在应用层 filter 分页
❌ 循环内 query / HTTP（N+1）
❌ 无上限 find + map 组装报表
❌ 每次请求 reload 全量缓存且无 version/TTL
```

```text
✅ WHERE/索引字段 + LIMIT/OFFSET 或 cursor
✅ 批量 IN 查询或 JOIN（关系库）/ 聚合管道（文档库）
✅ 计数用 COUNT/aggregate，不 pull 全表 length
✅ 热路径：缓存 + 失效；冷数据：异步任务
```

## 新增接口前检查

1. 查询能否用现有索引？不能 → 先索引或改查询，再写功能。
2. 是否会跨租户/跨表扫？→ 限制范围 + 分页。
3. 读多写少？→ 考虑短 TTL 缓存，写时失效。
4. 管理端报表？→ 独立只读接口或物化统计，不走 C 端热路径。

## 改 `index`/单体入口文件时

- 新路由优先拆到模块文件；不在单文件末尾无限追加。
- 中间件顺序、鉴权、tenant 上下文改动的副作用写清楚。
