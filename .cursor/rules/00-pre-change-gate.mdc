---
description: 任何项目改代码前必做：优化门禁与禁止「只堆页面」
alwaysApply: true
---

# 改代码前优化门禁（通用）

在**动手改文件之前**，必须先完成本清单；未完成不得提交实现方案或写代码。

## 1. 需求真实性（30 秒）

- 这是**新能力**、**修 bug** 还是**性能/结构债**？写一句话。
- 「完成」的定义：**端到端可用**（数据层 → API/服务 → 客户端），不是「页面能点」。
- 若只改 UI：必须说明数据从哪来、失败/空态/权限如何处理。

## 2. 影响面扫描（必读相关代码）

- 列出将改动的文件；**先读**调用链上下游（谁调这个接口、谁消费这个状态）。
- 搜索是否已有同类实现（hook/composable/service/util），**优先扩展**，禁止平行复制第三套逻辑。
- 估算数据量级：列表是否分页、是否会随用户/时间线性增长。

## 3. 性能与复杂度（必答，写在回复里）

| 问题 | 要求 |
|------|------|
| 读路径 | 能否用索引/主键/条件查询？禁止默认「拉全表再在内存 filter」除非数据有硬上限且已注释理由 |
| 写路径 | 是否必要事务/幂等？会否 N+1（循环里查库/请求）？ |
| 缓存 | 是否需失效策略？缓存多久、谁更新、脏读可接受吗？ |
| 前端 | 会否重复请求、轮询间隔、大列表虚拟滚动、图片懒加载/尺寸？ |
| 并发 | 多 tab、重复提交、竞态是否处理？ |

**任一列为「是风险」→ 必须先优化方案再写代码。**

## 4. 实现约束

- **最小 diff**：不扩 scope；不顺手重构无关文件。
- **禁止**：为赶工新增 `setInterval` 全量刷新、在组件里堆 200+ 行业务、复制粘贴改名的「新组件」。
- **优先**：专用接口 + 分页/筛选；复用现有抽象；可测试的纯函数。

## 5. 改后自检（交付前）

- [ ] 空数据 / 错误 / 慢网络 / 无权限 四种路径看过
- [ ] 列表 >50 条或会增长：有分页或明确上限
- [ ] 无新增「每次请求扫全库/全量 state」且无说明
- [ ] 无重复轮询、无未清理的 timer/listener
- [ ] 若无法本地压测：用文字说明预期 QPS/数据量与瓶颈点

## 6. 何时停止并先讨论

- 需要全表扫描、全量 hydrate、O(n×m) 嵌套循环才能完成功能
- 同一功能第二处复制粘贴
- 用户量上来必卡但短期无法改架构 → **先输出方案对比**，不直接堆代码
