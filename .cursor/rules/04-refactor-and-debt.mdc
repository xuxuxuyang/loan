---
description: 重构与降债：何时拆、何时停（通用）
alwaysApply: true
---

# 重构与技术债（通用）

## 允许小步重构的条件

- 与当前任务**同一文件/模块**且减复杂度
- 有行为等价说明；不混在功能 PR 里做大 bang 重写

## 必须停手、先出方案的情况

- 改一行要动 >10 个文件且无测试网
- 需要用「全量加载内存再算」才能上线
- 第三套相同逻辑的 utils/store/service

## 性能债偿还优先级

1. 扫全库 / 无分页热路径  
2. 未节流轮询、内存泄漏  
3. 巨型单体文件难维护  
4. 命名/格式

## Agent 行为

- 用户要「先实现再说」时：仍须完成 `00-pre-change-gate` 第 3 节表格；标出**已知风险**。
- 不引入新依赖解决已有库能做的事。
- 不生成未请求的文档/README；规则检查结论写在 PR/回复即可。
