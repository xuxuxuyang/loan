---
description: 功能「做完」的标准：禁止 UI 壳子式交付（通用）
alwaysApply: true
---

# 功能完成定义（非 UI 完成）

## 不算完成

- 只有页面/路由/按钮，接口 mock 或读本地假数据
- 列表写死数组、`.filter` 全量客户端分页冒充服务端分页
- 错误只 `console.log`，用户无提示
- 权限只藏菜单，接口仍返回敏感数据
- 「以后再加」的 TODO 留在主路径且无 issue/注释追踪

## 算完成（须同时满足）

1. **契约**：请求/响应字段、错误码、鉴权方式与文档或现有风格一致
2. **数据**：持久化或明确只读来源；写操作可追溯、可幂等（支付/下单类）
3. **规模**：默认按「1k 用户 / 10k 行」能工作；更大需分页/索引/异步
4. **可观测**：关键失败可定位（日志/错误信息，不含密码 token）
5. **可回滚**：大改动说明如何关开关或回退

## 交付时简短说明（3 行以内）

- 数据怎么读（一条 SQL/一次查询/缓存否）
- 最坏情况复杂度（O(?)）
- 已知限制与后续优化点（若有）
